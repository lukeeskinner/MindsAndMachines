# Copy to .env.local (gitignored) once the Cognito User Pool exists (see docs/AWS.md runbook).
# Leaving these unset keeps the demo fully anonymous — no Sign in button appears.
VITE_COGNITO_DOMAIN=your-pool-domain.auth.us-east-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=your-app-client-id
